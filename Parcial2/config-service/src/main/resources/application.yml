spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///D:/Poli/Cloud/Parcial2/Parcial2ConfigData
  security:
    user:
      name: ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}
server:
  port: 9000

